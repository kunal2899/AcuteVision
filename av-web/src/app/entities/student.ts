export class Student {
    constructor(public name:string, public mobile:number, public email:string, public enrollment:string, public year:number, public semster:number, public section:string, public department:string){}
}
